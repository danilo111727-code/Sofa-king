kind = "web"
previewPath = "/"
title = "Sofa King"
version = "1.0.0"
id = "artifacts/deploy-guide"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/deploy-guide run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/deploy-guide", "run", "build" ]
publicDir = "artifacts/deploy-guide/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
